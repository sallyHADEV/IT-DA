"""실행 계층.

1차에서는 :mod:`itda.engine.demo` 만 있다. 실제 실행 엔진(입력 주입, 이미지 매칭, 상황 워처,
멀티 플로우 스케줄러)은 2차에서 이 패키지에 들어온다.

두 경우 모두 GUI 와는 :mod:`itda.core.events` 의 이벤트 버스로만 이야기한다. 그래서 2차
엔진을 붙일 때 GUI 쪽 코드는 손대지 않아도 된다.
"""
